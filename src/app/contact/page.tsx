"use client";
import { SiteHeader } from "@/components/SiteHeader"; import { SiteFooter } from "@/components/SiteFooter"; import { PageHero } from "@/components/PageHero"; import { usePortfolio } from "@/context/PortfolioContext";
export default function Contact(){const {data}=usePortfolio(); return <><SiteHeader/><main><PageHero eyebrow="GET IN TOUCH" title="Let’s Connect" description="Open to professional conversations about quality engineering, enterprise delivery, automation, mentoring and leadership."/><section className="shell contact-grid"><article className="bento-card large"><h2>Professional Contact</h2><p><strong>Email</strong><br/><a href={`mailto:${data.profile.email}`}>{data.profile.email}</a></p><p><strong>Location</strong><br/>{data.profile.location}</p><div className="actions"><a className="button" href={data.profile.linkedin} target="_blank">LinkedIn ↗</a><a className="button secondary" href={data.profile.github} target="_blank">GitHub ↗</a></div></article><article className="bento-card large"><h2>Send a message</h2><p className="muted">GitHub Pages has no server-side mail service in this first release. This form opens your email client with the message prepared.</p><ContactForm email={data.profile.email}/></article></section></main><SiteFooter/></>}
function ContactForm({email}:{email:string}){function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault(); const f=new FormData(e.currentTarget); const subject=encodeURIComponent(String(f.get('subject')||'Portfolio enquiry')); const body=encodeURIComponent(`Name: ${f.get('name')}\n\n${f.get('message')}`); window.location.href=`mailto:${email}?subject=${subject}&body=${body}`;} return <form className="form" onSubmit={submit}><input name="name" placeholder="Your name" required/><input name="subject" placeholder="Subject" required/><textarea name="message" placeholder="Your message" rows={7} required/><button className="button" type="submit">Prepare Email</button></form>}
