"use client"; import { usePortfolio } from "@/context/PortfolioContext";
export default function ExperienceEditor(){const {data,update}=usePortfolio(); return <><div className="setup-header"><div><span className="eyebrow">EXPERIENCE</span><h1>Career Timeline</h1><p>Edit the roles shown in “A Journey of Growth”.</p></div></div><section className="admin-list">{data.experience.map((e,i)=><article className="admin-card experience-editor" key={`${e.role}-${i}`}><div className="two-col"><label>Role<input value={e.role} onChange={ev=>update(d=>{d.experience[i].role=ev.target.value})}/></label><label>Company<input value={e.company} onChange={ev=>update(d=>{d.experience[i].company=ev.target.value})}/></label><label>Start<input value={e.start} onChange={ev=>update(d=>{d.experience[i].start=ev.target.value})}/></label><label>End<input value={e.end} onChange={ev=>update(d=>{d.experience[i].end=ev.target.value})}/></label></div><label>Summary<textarea rows={3} value={e.summary} onChange={ev=>update(d=>{d.experience[i].summary=ev.target.value})}/></label></article>)}</section></>}
