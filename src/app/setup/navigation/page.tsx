"use client"; import { usePortfolio } from "@/context/PortfolioContext";
export default function NavigationEditor(){const {data,update}=usePortfolio(); return <><div className="setup-header"><div><span className="eyebrow">NAVIGATION</span><h1>Menu Visibility</h1><p>Choose which major portfolio pages appear in public navigation.</p></div></div><section className="admin-card nav-editor">{data.navigation.map((n,i)=><div className="nav-row" key={n.href}><div><strong>{n.label}</strong><small>{n.href}</small></div><label className="switch"><input type="checkbox" checked={n.visible} onChange={e=>update(d=>{d.navigation[i].visible=e.target.checked})}/><span>{n.visible?'Visible':'Hidden'}</span></label></div>)}</section></>}
