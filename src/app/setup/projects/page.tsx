"use client"; import { usePortfolio } from "@/context/PortfolioContext";
export default function ProjectsEditor(){const {data,update}=usePortfolio(); function add(){update(d=>d.projects.unshift({id:`project-${Date.now()}`,title:"New Project",category:"Other",description:"Add a professional project description.",tools:[],featured:false}))} return <><div className="setup-header"><div><span className="eyebrow">PROJECTS</span><h1>Project Manager</h1><p>Add, edit, feature or remove portfolio work.</p></div><button className="button compact" onClick={add}>+ Add Project</button></div><section className="admin-list">{data.projects.map((p,i)=><article className="admin-card project-editor" key={p.id}><div><input className="title-input" value={p.title} onChange={e=>update(d=>{d.projects[i].title=e.target.value})}/><input value={p.category} onChange={e=>update(d=>{d.projects[i].category=e.target.value})}/><textarea rows={3} value={p.description} onChange={e=>update(d=>{d.projects[i].description=e.target.value})}/></div><div className="project-controls"><label className="check"><input type="checkbox" checked={p.featured} onChange={e=>update(d=>{d.projects[i].featured=e.target.checked})}/> Featured</label><button className="danger" onClick={()=>update(d=>{d.projects.splice(i,1)})}>Remove</button></div></article>)}</section></>}
