"use client";
import { usePortfolio } from "@/context/PortfolioContext";
import Link from "next/link";
export default function SetupDashboard(){const {data}=usePortfolio(); const checks=[Boolean(data.profile.name),Boolean(data.profile.email),Boolean(data.profile.cv),data.projects.every(p=>Boolean(p.description)),data.experience.length>0,Boolean(data.seo.title),Boolean(data.seo.description)]; const score=Math.round(checks.filter(Boolean).length/checks.length*100); return <><div className="setup-header"><div><span className="eyebrow">PORTFOLIO CONTROL CENTER</span><h1>Dashboard</h1><p>Manage content, review portfolio health and prepare publishing exports.</p></div><Link className="button compact" href="/">Preview Site ↗</Link></div><section className="setup-cards"><article className="admin-card health"><div className="health-ring" style={{'--score':`${score}%`} as React.CSSProperties}><strong>{score}%</strong></div><div><h2>Portfolio Health</h2><p>{score>=90?'Excellent':'Needs attention'}</p></div></article><article className="admin-card stat"><strong>{data.projects.length}</strong><span>Projects</span></article><article className="admin-card stat"><strong>{data.experience.length}</strong><span>Experience</span></article><article className="admin-card stat"><strong>{data.skills.reduce((n,g)=>n+g.items.length,0)}</strong><span>Skills</span></article></section><section className="admin-grid"><article className="admin-card"><h2>Health Checks</h2><ul className="check-list"><li>✓ Profile configured</li><li>✓ Contact information available</li><li>✓ CV asset configured</li><li>✓ Published project descriptions</li><li>✓ Experience timeline available</li><li>✓ SEO metadata configured</li></ul></article><article className="admin-card"><h2>Publishing Model</h2><p>GitHub Pages edition stores edits as a browser draft. Export JSON after editing and commit the configuration when you want to publish permanently.</p><p className="notice">Future cloud mode can save these same screens directly to an authenticated API/database.</p></article></section></>}
