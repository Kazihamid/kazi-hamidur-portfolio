"use client"; import { usePortfolio } from "@/context/PortfolioContext";
export default function HomeEditor(){const {data,update}=usePortfolio(); const p=data.profile; const set=(k:keyof typeof p,v:string)=>update(d=>{(d.profile as Record<string,string>)[k as string]=v}); return <><div className="setup-header"><div><span className="eyebrow">HOME</span><h1>Edit Homepage</h1><p>Control the public homepage hero and key message.</p></div></div><section className="editor-grid"><div className="admin-card form"><label>Eyebrow<input value={p.eyebrow} onChange={e=>set('eyebrow',e.target.value)}/></label><label>Main Title<input value={p.heroTitle} onChange={e=>set('heroTitle',e.target.value)}/></label><label>Description<textarea rows={6} value={p.heroDescription} onChange={e=>set('heroDescription',e.target.value)}/></label></div><div className="admin-card live-card dark-preview"><span className="eyebrow">{p.eyebrow}</span><h2>{p.heroTitle}</h2><p>{p.heroDescription}</p></div></section></>}
